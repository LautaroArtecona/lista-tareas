import express from 'express'

const router=express.Router()

/* router.get('/',(req,res)=>{
    Item.find({},function(error,foundItems){
        res.render('pages/list',{newListItems:foundItems})
    })
}) */



/* app.get('/',(req,res)=>{

})  al estar en la carpeta de router, no se usa mas el "app"*/

export default router